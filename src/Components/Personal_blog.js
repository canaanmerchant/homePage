import React, { Component } from 'react';
import PostList from './PostList';

const Personal_blog = ({title, body, id})  => {
  return (
      <div class= "f3 tc">
        <PostList />
     </div>
  );
}

export default Personal_blog;
